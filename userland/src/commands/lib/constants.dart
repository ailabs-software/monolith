

// When the browser requests "GET /", the super frame is loaded. Specify the path the correct super frame to use.
const String SUPER_FRAME_PATH = "/~/system/frames/frame_manager";

const int TERMINAL_PORT = 80;
